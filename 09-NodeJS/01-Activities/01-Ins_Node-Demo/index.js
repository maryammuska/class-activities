// Run this from the command line using 'node index.js'
(function () {
  console.log('this', this);
})(); //the last () is called an IIFE
// this just makes it run through the process immediately (not common)

console.log('1 + 2', 1 + 2);
